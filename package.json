{
  "name": "@widgets/monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r --filter=\"./packages/*\" run build",
    "start": "pnpm -r --filter=\"./packages/codecorner\" run start"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  }
}
